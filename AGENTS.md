# 코딩 에이전트 지침 — alpha_trader

이 저장소는 **public** 투자 스터디 포트폴리오 트래커다. 원칙은 README에 있다:
비중(%)만 공개하고 계좌 금액은 어디에도 없어야 한다.

## 절대 쓰지 말 것

PR 본문, 커밋 메시지, JSON `note` 필드, 이슈, 코멘트 — 이 저장소와 관련된
어떤 텍스트에도 다음을 쓰지 않는다.

- 계좌 평가액·잔고·잔액·예수금
- 매수액·매도액·원금·총자산 (원화든 달러든)
- 보유 주수·매입 단가 (예: "6주", "34.28주", "주당 464,403원")
- 계좌명(토스, 키움 등 특정 증권사·플랫폼 이름과 계좌 규모를 연결하는 표현)

비중 산출 근거를 설명할 때는 "실측 평가액을 앵커로 역산" 같은 **방법**만 쓰고
숫자 자체는 생략한다. 스크린샷에서 원본 수치를 그대로 옮겨 적지 않는다.

숫자를 반드시 언급해야 하는 유일한 예외는 **비중(%)** 이다 — 이건 이 저장소의
존재 이유이므로 얼마든지 써도 된다 (`weight: 31.87` 같은 것).

## 커밋 전 자기 검사

```bash
python3 tools/guard.py --stdin < 메시지파일
```

커밋 메시지·PR 본문을 파일로 먼저 쓰고 위 명령으로 통과하는지 확인한 뒤 사용한다.
`.github/workflows/guard.yml` 이 PR 제목·본문·커밋 메시지·diff 를 동일한 방식으로
자동 검사하므로, 여기서 걸러지지 않아도 CI 에서 반려된다.

## 데이터 파일 규칙

`data/members/**/*.json` 은 `tools/validate.py` 가 검증한다. README 스키마에
없는 키(특히 amount/value/balance/qty/shares 류)는 통째로 반려되고, `note` 필드는
`tools/guard.py` 로 스캔된다. 스키마를 벗어나는 필드를 임의로 추가하지 않는다.

## 허용

커밋 트레일러의 `Co-Authored-By:` 는 금액·주수가 아니므로 허용한다.

## 관련 파일

- 검사 로직: `tools/guard.py` (표준 라이브러리만, `scan(text)` 로 재사용 가능)
- 검증 통합: `tools/validate.py`
- CI: `.github/workflows/guard.yml`
